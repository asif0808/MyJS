const mp=new Map()
mp.set("name","Aasif")
mp.set("Dept","CSE")
for (const element of mp) {
    console.log(element);
    
}
// using for..of on array
const arr=[3,4,52,true]
for (const element of arr) {
    console.log(element);
}