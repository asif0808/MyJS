// defining a string
// can be defined using let, var or const
// string can also be defined as object of String class
const name="john"
console.log(`Hello ${name}`)
// string functions
let greet=" Hello_World_This_is_Me"
console.log(greet.length, typeof greet.length)
